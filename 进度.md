# tech-news-hub · 进度文档

> 给项目主人看的"人话"进度。机器状态在 `.loop-state.json`，长期记忆在 `.workbuddy/memory/MEMORY.md`。
> 本文件每次迭代**实时更新**，断点续跑不丢上下文。

## 项目目标
每周更新的科技资讯网站：聚焦 AI 与科技圈，打破信息差，让非专业用户真正读懂本周科技圈发生了什么、与过去有何不同、对行业与自身的影响及可采取的行动。每条资讯含 8 维度（标题摘要 / 发生了什么+来源 / 关键对比 / 为什么发生 / 产出了什么 / 通俗讲解 / 行业影响 / 读者行动建议），来源真实可引用、多维度交叉验证。支持周次归档浏览、分类切换（AI圈/科技圈/可扩展其他圈层），持续向可部署可分享的应用层靠拢。

## 当前状态
- 阶段：**R1–R18 全部完成**；已发布 **GitHub Pages 公开站** `https://huanweide.github.io/tech-news-hub/`（0 元、自动部署）+ 源码仓库 `huanweide/tech-news-hub`（默认分支 master）；按钮契约 39/39 + 架构渲染 31/31 + 术语弹层 13/13 + 资料库 32/32 + 优惠圈 19/19 + **LLM 助手 31/31** + **npm test 126 项断言全绿**；**独特定位 = 新闻性 + 学习性 + 可对话 + 可省钱（BYOK·本地密钥·检索增强 AI 助手）**。
- 已完成：R1–R16 全部能力（平台重构/暗色/搜索/行动清单/分享长图/8维度展开+18架构图/R14 91词术语弹层/R15 资料库·索引·每周自动化/R16 右侧 LLM 助手 BYOK·检索增强）+ **R17 发布到 GitHub Pages**：git 初始化、`.gitignore` 排除 `.workbuddy/`·`.loop-state.json`·`node_modules/`（不泄露内部状态/依赖）、`README.md`、`package.json`、`scripts/weekly_update.cjs`、`.github/workflows/weekly-update.yml`；**两层自动更新**：① WorkBuddy 自动化（每周一 09:00，本模型高质量更新 + git push，Pages 自动部署）② GitHub Actions（每周日 22:00 UTC，RSS 兜底抓取 + 可选 `DEEPSEEK_API_KEY` AI 摘要 + push）；`npm test` 为推送前质量门。
- 待命：产品已达"权威深读型可对话可薅羊毛可公开访问成品"——R18 模型优惠圈已上线(独立 deals 视图 + 客户端自动过期下架 + 类型/平台/关键词过滤 + 后端幂等脚本 + 助手 lookup_deal 第6工具，全栈 126 项断言全绿)；第十九轮候选见最下方（axe-core 无障碍回归 / 分享长图含助手对话与优惠圈 / 技术演进时间线 / 更多分类真实数据 / 优惠圈真实抓取配 DEEPSEEK_API_KEY）。

## 怎么跑起来（给主人）
- **在线访问（已发布）**：https://huanweide.github.io/tech-news-hub/  （GitHub Pages，0 元，每周自动更新）
- **源码仓库**：https://github.com/huanweide/tech-news-hub
```
cd tech-news-hub
# 本地预览：纯静态站点（数据在 news-data.js，无需后端）
python -m http.server 8000
# 浏览器打开 http://localhost:8000
# 点顶部分类（AI圈/科技圈）切换；点 📚资料库 进知识库；点 🤖助手 配置你的 API 密钥开启本地 AI 导师
# 自动更新：每周自动抓取真实新闻并推送，无需手动维护
```

## 迭代日志
### 2026-07-25 第一轮（已完成）
- [x] init：锚定目标 + 固化魔王工作流到 MEMORY.md
- [x] 写入两条上下文铁律到 MEMORY.md（先查记忆再动手 / 实时更新进度）
- [x] 抓取本周真实资讯 8 条（WebSearch 多源交叉验证：Kimi K3 / Qwen3.8 / 混元Hy3登顶OpenRouter+Hyra / 端侧AI备案 / 星舰试飞取消 / 硅基量子阵列 / 中国聚变+二维半导体 / MIT光子芯片，附来源链接）
- [x] 数据层 news-data.js（周次 / 分类 / 8维度 / 来源 结构化；长文本不进主上下文）
- [x] 前端 index.html + app.js（周次归档栏 + 分类切换 + 资讯流 + 卡片8维度展开 + 暗色切换）
- [x] 样式 style.css（设计系统、浅/暗双主题、响应式）
- [x] 模拟终端用户验证：HTTP 资源全 200、数据层 8维度+来源完整性通过、过滤逻辑正确（AI第3周3条/科技第4周1条）、4 处交互绑定 → SIMULATE_PASS
- [x] 自我修正：发现 `loop_engine.simulate` 对纯前端项目误判（单元素匹配 + 强依赖后端路由），已修正为支持多元素逗号分隔 + 自动识别纯前端并判 N/A 通过，并重打包技能
- [x] 四问自检写入 loop 状态

### 2026-07-26 第二轮（已完成 · 增强版）
- [x] 数据层：8 条资讯全部加 `tags[]` 与 `impactScore`(1–100)，更新字段契约注释
- [x] 顶栏：新增全文搜索框（标题/摘要/8维度/标签）+ 周报导出按钮
- [x] 工具栏：影响力降序排序切换 + 已选标签 chips（可点✕移除）+ 结果计数
- [x] 卡片：标签 chip 可点叠加过滤(AND) + 影响力评分条
- [x] 暗色偏好 localStorage 持久化（刷新不丢）
- [x] 周报导出 Markdown（一键下载当前筛选结果，含全部 8 维度与来源）
- [x] 模拟终端用户验证：资源全 200、数据完整性、全部交互绑定 → SIMULATE_PASS；四问写入状态机

### 2026-07-25 第三轮（已完成 · UX 精修 · 13/13 模拟通过）
- [x] 先按铁律读记忆/进度，再定位痛点（第二轮走查已暴露 A–J 十项）
- [x] **A 跨分类搜索智能提示**：当前分类 0 结果但其它分类有命中时，空状态展示可一键切换的分类按钮（保留搜索与标签），如 AI圈搜"量子"→提示科技圈 2 条
- [x] **B 标签 AND 解释**：0 结果且已选标签时，明确说明"同时满足（AND）"语义
- [x] **C 影响力图例**：工具栏常驻 subhint 解释"影响力 1–100 分，越高越值得优先读" + 卡片 title 提示
- [x] **D 导出 toast 反馈**：导出成功/失败改为非阻塞 toast，替代 alert
- [x] **E 禁用分类视觉区分**：生命医学圈/能源圈加"即将开放"虚线徽标 + 标题提示，不再是"像 bug"
- [x] **F 首屏上手引导**：默认态展示"30 秒上手"卡片，一旦搜索/筛选自动隐藏，可关闭
- [x] **G 空状态整合计数**：空卡片内直接显示"共 0 条"与可操作建议 + 清除筛选按钮
- [x] **I 卡片 meta 视觉分组**：左（分类/周次/标签）右（影响力/来源）分组，密度更清晰
- [x] 模拟验证：复现过滤/空状态/跨分类逻辑断言全绿；http.server 起服务 4 资源全 200
- [x] 四问写入 loop 状态机

### 2026-07-25 第四轮（已完成 · 细节打磨 H/J 收尾）
- [x] **H 响应式**：窄屏(≤520px)顶栏操作区竖排，搜索占满行、导出/暗色并排；meta 右组在窄屏取消右推
- [x] **J 无障碍**：feed 与 toast 加 aria-live（动态内容变更播报）；按钮/输入/标签/开关统一 focus-visible 键盘焦点环；展开按钮带 aria-expanded；标签 chip 带 aria-pressed
- [x] **视觉**：暗色模式 toast 反色保证对比度；空状态/引导卡片双主题适配
- [x] 产品体验验收完毕，进入"可部署成品"状态

### 2026-07-25 第五轮（已完成 · 平台级重构）
- [x] 信息架构平台化：吸顶 frosted 头部 + 分类药丸导航 + Hero 头条 + 话题标签栏 + 主网格封面卡(渐变封面+图标+影响力进度条) + 侧栏(周次归档/编辑精选/热度榜 Top5) + 订阅 CTA + 页脚
- [x] 新增 `features.js`：`TechNewsFeatures.recommend(itemId, items, n)`（共享标签+同分类/同周兜底+影响力综合打分）与 `trending(items, n)`（影响力降序），纯函数、零依赖、带防御
- [x] 设计系统升级：CSS 变量 token、双主题、渐变封面、徽标、响应式断点；卡片错峰淡入、Hero 淡入
- [x] 模拟终端用户验证：复用过滤/推荐/热度逻辑 15 项断言全绿（Hero 选取/网格过滤/标签 AND/跨分类/相关阅读/趋势/防御）；5 资源全 200
- [x] 四问写入状态机

### 2026-07-26 第六轮（已完成 · 细节精修）
- [x] 封面影响力进度条 `.cov-bar`；卡片错峰淡入 `fadeIn` + Hero 淡入 `fadeUp`
- [x] 回到顶部按钮 `#toTop`（滚动 >400px 浮现，平滑回顶）
- [x] 视觉：暗色 toast 反色、空状态/引导双主题适配；app.js 语法 APP_OK；5 资源全 200
- [x] 产品达"平台级成品"体验，进入"内容扩充 + 可视化 + 部署"阶段

### 2026-07-25 第七轮（已完成 · 可视化 + 内容扩充 + 部署上线）
- [x] **内容扩充（Agent 研究）**：派发研究 Agent 用 WebSearch 抓取本周 w4 真实资讯 4 条——①《AI拟人化互动服务管理暂行办法》施行(AI监管) ②工信部/国资委人形机器人实景实训"量产元年"(具身智能) ③引力一号遥四海上发射"一箭九星"(商业航天) ④原子矩阵中性原子量子整机2310量子比特(量子计算)；全部含 8 维度+权威来源；本周 w4 从 2 条→6 条，总条目 8→12
- [x] **影响力透视（Chart.js）**：侧栏新增"📊 影响力透视·本周vs上周"柱状图（AI圈/科技圈 × 本周/上周 平均影响力）；CDN 引入并做无网优雅降级（fallback 文案）
- [x] **本周速览数据条**：主区新增数据条——本周 N 条 / 平均影响力 M（▲▼对比上周）/ 本周头条，强化"新闻平台"数据感
- [x] **部署上线**：CloudStudio 静态部署成功，产出可分享链接 `https://ffab6d47aa324857901a2e5ca6af496f.app.codebuddy.work`
- [x] 模拟验证：node --check 语法通过；sim 复刻图表统计(aiW4=87/techW4=78/aiW3=88/techW3=88, w4avg=82 vs w3avg=88)与 12 条数据完整性 10/10 断言；http.server+curl 5 资源全 200
- [x] 四问写入状态机

### 2026-07-25 第八轮（已完成 · 搜索高亮 + 空状态随机看 + 本周NEW徽标）
- [x] **搜索关键词高亮**：新增 `escapeHtml`/`hl` 辅助函数（防 XSS），应用于卡片标题/摘要、Hero、8 维度详情；大小写不敏感、特殊字符不崩、恶意查询不注入
- [x] **空状态"随机看一条"**：无结果时除"清除筛选"外新增 🎲 随机看一条，直接跳到随机条目，把死路变活路（容错/认知减负）
- [x] **本周 NEW 徽标**：本周(w4)条目加 🆕 本周 徽标，强化"新鲜资讯"平台感
- [x] **重新部署**：CloudStudio 重新部署生效（同一沙箱 `ffab6d47...`）
- [x] 模拟验证：node --check app.js 语法通过；sim 断言 escapeHtml/hl(7/7) 与 NEW 判定/随机看；http.server+curl 5 资源全 200
- [x] 四问写入状态机

### 第九轮候选（来自第八轮四问，待续）
- **还能做**：RSS/NewsAPI 自动抓取管线（每周自动更新数据）/ 部署持续监测与回链 / 更多分类(生命医学/能源)真实数据 / 移动端再打磨
- **能加**：影响力趋势折线(跨周) / 资讯卡片分享长图 / 读者行动 checklist / 搜索结果实时计数
- **提升思维**：从"功能完整"升到"抗噪与容错"——空状态随机看与搜索高亮都是降低用户迷茫、把死路变活路的认知减负
- **能集成**：RSS/NewsAPI 抓取 / model_router 调 LLM 自动生成通俗讲解与行动初稿 / html2canvas 分享长图 / axe-core 无障碍回归 / GitHub Pages·Vercel 多渠道部署

### 2026-07-25 第八轮（补记 · 已完成）
- [x] 搜索关键词高亮（escapeHtml/hl 防 XSS，应用于卡片/Hero/8 维度详情；大小写不敏感、特殊字符不崩、恶意查询不注入）
- [x] 空状态新增「🎲 随机看一条」按钮（把死路变活路）；本周(w4)条目加 🆕 本周 NEW 徽标
- [x] 重新部署 CloudStudio 生效（同一沙箱 ffab6d47...）

### 2026-07-25 第九轮（已完成 · 搜索实时计数 + 我的行动清单）
- [x] **搜索实时计数**：搜索框右侧新增「🔍 N 条匹配」徽标，q 为空隐藏、0 结果标红；补全 R8 高亮的即时反馈闭环
- [x] **我的行动清单**：每条资讯「读者行动建议」新增「＋加入我的行动清单」按钮；侧栏新增 widget 列出已加条目，可勾选「已完成」、显示「已完成 X/Y」进度、可移除、点击跳转回该资讯；状态存 localStorage(`techpulse-actions`) 跨会话持久化
- [x] **重新部署**：CloudStudio 重新部署生效（同一沙箱 ffab6d47...），分享链接不变 `https://ffab6d47aa324857901a2e5ca6af496f.app.codebuddy.work`
- [x] 模拟验证：node --check 语法通过；sim 复刻过滤/搜索计数语义与行动清单逻辑 137/137 全绿；http.server+curl 5 资源全 200
- [x] 四问写入状态机

### 第十轮候选（来自第九轮四问，待续）
- **还能做**：影响力趋势折线(跨周) / 更多分类(生命医学/能源)真实数据 / RSS·NewsAPI 自动抓取管线 / 部署持续监测与回链 / 移动端再打磨
- **能加**：资讯卡片分享长图(html2canvas) / 行动清单按周归档与导出 / 周报 PDF 导出
- **提升思维**：从"信息可懂"升到"信息可行动"——把读者行动建议从被动文本变主动可勾选清单，是费曼"学以致用"的落地
- **能集成**：html2canvas 分享长图 / model_router 调 LLM 自动生成通俗讲解与行动初稿 / RSS·NewsAPI 抓取 / axe-core 无障碍回归 / GitHub Pages·Vercel 多渠道部署

### 2026-07-25 第十轮（已完成 · 影响力走势跨周折线）
- [x] **影响力走势折线**：在 R7「本周 vs 上周」柱状图旁新增「📈 影响力走势（跨周）」折线图（Chart.js），展示 AI圈(88→87) / 科技圈(88→78) / 整体平均(88→82) 两周变化方向；与柱状图并列互补（对比 vs 走势），让用户一眼看出"哪个圈在升温/降温"
- [x] **重新部署**：CloudStudio 重新部署生效（同一沙箱 ffab6d47...），分享链接不变
- [x] 模拟验证：node --check 语法通过；sim 复刻趋势统计 + 数据完整性 129/129 全绿；http.server+curl 5 资源全 200
- [x] 四问写入状态机

### 2026-07-26 第十一轮（已完成 · 用户指令：非盈利 + 导入本周新闻 + 逐按钮确认可交互）
- [x] **非盈利标识**：头部新增「非盈利」徽标；页脚新增明确声明「本站为非盈利公益科普平台：内容仅供学习参考，不投放任何广告、不接入商业变现、不向用户收费；所有资讯均附可追溯来源」
- [x] **导入本周真实新闻（w5 · 7/20–7/26）**：派两个研究 Agent 用 WebSearch 抓取本周真实可引用资讯，按既有 8 维度格式（what/compare/why/output/explain/impact/action + sources）导入 —— AI圈3条：①北京智能体专项政策(最高1亿/首提AIP与Token经济) ②Google Gemini 3.6 Flash + 代码安全智能体 CodeMender ③WAIC 2026 闭幕(300+首发/国产开源出海)；科技圈3条：④HIAF 强流重离子加速器建成投运 ⑤力箭一号遥十五一箭五星(含首颗商业空间碎片监测卫星) ⑥我国脑机接口实现全球首次跨地域千人级同步脑电采集。每条均附 2–3 个权威来源
- [x] **周次重构**：新增 w5(2026年7月第5周)；默认周次=最新周；「本周」NEW徽标/影响力柱状图/走势折线/本周速览 全部动态指向最新两周（不再硬编码 w4），历史 w3/w4 保留为归档
- [x] **致命 bug 修复（关键）**：发现 news-data.js 仅用 `const NEWS_DATA` 声明、从未赋值 `window.NEWS_DATA`，而 app.js 用 `window.NEWS_DATA` 取数据 → 线上整页脚本加载即崩溃、所有按钮无响应（此前 R1–R10 线上版本均受影响）。已补 `window.NEWS_DATA = NEWS_DATA`，修复后全站功能恢复
- [x] **逐按钮真机验证（用户硬性要求）**：用 jsdom 加载真实 index.html+app.js，对每一个按钮/交互控件派发真实点击事件，断言可见响应 —— 分类切换/周次归档/排序/搜索+实时计数/标签AND/卡片与Hero展开8维度/加入·勾选·移除行动清单/相关阅读跳转/导出周报/暗色切换/订阅表单/回到顶部/引导关闭/空状态随机看+清除筛选/跨分类切换 共 **39/39 全绿**
- [x] **重新部署**：CloudStudio 重新部署生效（同一沙箱 ffab6d47...），分享链接不变 `https://ffab6d47aa324857901a2e5ca6af496f.app.codebuddy.work`
- [x] 四问写入状态机

### 第十二轮候选（来自第十一轮四问，待续）
- **还能做**：更多分类(生命医学/能源)真实数据 / RSS·NewsAPI 自动抓取管线 / 部署持续监测与回链 / 移动端再打磨
- **能加**：资讯卡片分享长图(html2canvas) / 行动清单按周归档与导出 / 周报 PDF 导出 / 趋势图支持更多周次累积
- **提升思维**：从"单点对比"(柱状)升到"时间序列认知"(折线)——折线让用户一眼看出趋势，是趋势思维的落地
- **能集成**：html2canvas 分享长图 / model_router 调 LLM 自动生成通俗讲解与行动初稿 / RSS·NewsAPI 抓取 / axe-core 无障碍回归 / GitHub Pages·Vercel 多渠道部署

### 2026-07-26 第十二轮（已完成 · 分享长图 html2canvas）
- [x] **资讯卡片分享长图**：新增 `shareLongImage()`（html2canvas，CDN 引入 + 无网优雅降级）；生成 620px 宽主题化长图（非盈利标识 + 头条 + 列表 + 来源 + 公益声明），一键下载 PNG。
- [x] 重新部署 CloudStudio 生效（同一沙箱 ffab6d47...），分享链接不变 `https://ffab6d47aa324857901a2e5ca6af496f.app.codebuddy.work`。
- [x] 模拟验证：node --check 通过；sim 复刻分享降级与导出逻辑；5 资源全 200。
- [x] 四问写入状态机。

### 2026-07-26 第十三轮（已完成 · 用户指令：权威化重写 + 架构图 + 去生硬结构）
- [x] **权威化重写全部 18 条新闻**：AI圈(w3/w4/w5 共9) + 科技圈(共9) 全部重写为严肃、权威、带机制/架构/权衡/术语深度的分析体，去除"大白话/零基础"口吻。
- [x] **升级 `explain` 维度**：由"通俗讲解"改为"技术解析与架构"，聚焦机制/架构/对比，不再用费曼式通俗类比。
- [x] **去生硬结构**：将详情区 `<h4>` 维度小标题统一改为克制的眉标 `.dim-kicker`（小渐变竖条 + 标签字），不再"一个个小标题"死板排列；引用来源标题同步软化。
- [x] **新增 18 张内联架构图**：每条新闻的 `技术解析` 后附内联 SVG 架构示意（`architecture` + `archCaption` 字段），用 `var(--…)` 主题色，浅/暗主题自动适配，无需双份资源；涵盖 MoE、多模态+工具链、端云协同、递归自我改进、火箭级叠、硅基量子、三大根技术、光子芯片、监管架构、具身智能体闭环、海上发射、中性原子、智能体政策生态、Gemini+CodeMender、WAIC 全栈、HIAF 加速链、在轨智能、脑机接口数据湖。
- [x] **数据层修复**：重写时遗漏的 `sources` 一次性补回（18/18），恢复"可追溯来源"与 Hero 渲染。
- [x] **tagline** 改为权威定位：「深度技术研判 · 非盈利公益科普平台｜逐条拆解底层机制、架构与产业逻辑，附可追溯来源」。
- [x] **模拟验证（r13_test.cjs，jsdom 真机派发）**：18/18 架构 SVG 渲染 + 18/18 archCaption + 8 维度字段契约 + SVG 用 var(--…) + 眉标存在且无 `.dim h4` + 展开后 arch-fig 含合法 viewBox 与 figcaption + 全部按钮契约 0 回归 → **31/31 ALL_GREEN**。
- [x] **重新部署** CloudStudio 生效（同一沙箱 ffab6d47...），分享链接不变；5 资源全 200。
- [x] 四问写入状态机（q3：从"信息可懂"升到"信息可信且专业"，用架构图把抽象机制变可视图，是"可视化即解释"的落地）。

### 第十四轮候选（来自第十三轮四问，待续）
- **还能做**：专业术语词典弹层（点击术语看权威释义）/ 更多分类(生命医学/能源)真实数据 / 架构图进分享长图 / 趋势图累积更多周次。
- **能加**：术语表侧栏 widget / 相关阅读按技术同源性推荐 / 周报 PDF 导出 / 行动清单按周归档。
- **提升思维**：从"信息专业"升到"信息可深读"——术语弹层让读者在权威长文中随时获得精确定义，是把"专业"真正落到可读性的关键。
- **能集成**：axe-core 无障碍回归 / html2canvas 把架构 SVG 一并绘入长图 / RSS·NewsAPI 自动抓取 / model_router 调 LLM 自动生成行动初稿 / GitHub Pages·Vercel 多渠道部署。

### 2026-07-26 第十四轮（已完成 · 专业术语词典弹层）
- [x] **专业术语词典弹层**：app.js 新增 `GLOSSARY`（57 个权威术语释义，覆盖 MoE/门控网络/激活参数/开放权重/蒸馏/端侧/端云协同/NPU/量化/备案/多模态/统一表征/智能体/具身智能/递归自我改进/OpenRouter/RL/一箭多星/上面级/硅基量子/超导量子比特/量子纠错/中性原子/光镊/量子比特/光子芯片/光互连/硅光/HIAF/脑机接口/BCI/神经信号/数据湖/人形机器人/AIP/Token 经济 等）。
- [x] **术语高亮包裹**：新增 `gloss()`，在搜索高亮 `hl()` 之后把正文中的术语包成可点击 `.term`（虚线下划线，浅/暗主题自适应）；按术语长度降序匹配，避免短词在长词内部重复包裹；详情/Hero/卡片 标题与正文统一经 `gh()=gloss(hl())` 渲染。
- [x] **点击/聚焦释义弹层**：点击或键盘聚焦 `.term` 弹出 `#glossPop`（标题 + 权威释义，视口内定位，外部点击 / Esc 关闭，键盘可达），把"专业"真正落到可读性——读者随时能拿到精确定义。
- [x] **修复真机抛错**：`showGloss` 误用 `win`/`doc`（app.js 作用域内未定义）导致每次点术语抛 ReferenceError；改为 `window`/`document`，真机不再报错。
- [x] **模拟验证（r14_test.cjs，jsdom 真机派发）**：13/13 全绿——术语渲染 / 点击弹层 / 外部关闭 / 聚焦可达 / Esc 关闭 / 与搜索高亮共存不抛错 / 架构图与眉标结构未破坏；r13_test.cjs 31/31 零回归。
- [x] **重新部署** CloudStudio 生效（同一沙箱 ffab6d47...），分享链接不变；5 资源全 200。
- [x] 四问写入状态机（q3：从"信息专业"升到"信息可深读"，专业不等于堆术语，而是读者随时能拿到精确定义）。

### 第十五轮候选（来自第十四轮四问，部分已在本轮落地）
- **还能做**：资料库"技术演进时间线"把同主题(如 MoE/量子)跨周串联 / 术语按"难度/领域"标签分组 / 新闻库按影响力·周次排序 / 资料库条目一键分享长图。
- **能加**：架构图点击节点高亮关联术语 / 每架构补"一句话原理"摘要卡 / 更多分类(生命医学/能源)真实数据并纳入 GLOSSARY / 资料库离线 PWA 缓存。
- **提升思维**：从"新闻可读"升到"知识可查"——新闻一次性消费，资料库把每篇新闻沉淀的概念变成可检索、可导航、可复用的知识资产，是"学习性+新闻性"独特定位的真正落地。
- **能集成**：axe-core 无障碍回归 / html2canvas 把资料库术语卡·架构图绘入分享长图 / Mermaid·Excalidraw 自动生成架构图并挂术语链接 / model_router 调 LLM 自动抽术语生成 GLOSSARY 初稿 / RSS·NewsAPI 自动抓取 / 自动化"每周一更新"已落地。

### 2026-07-26 第十五轮（已完成 · 资料库/索引/自动更新 — 用户："做成具有学习性和新闻性的网站，真正有价值"）
- [x] **资料库入口 + 三栏索引**：顶部新增 📚资料库 按钮；进入后替代资讯流，呈现"名词索引 / 架构索引 / 新闻库"三栏可检索知识库（`.kb-open` 切换）。
- [x] **名词索引 → 名词详情**：`GLOSSARY` 扩充至 **91 条**（新增 RAG/LoRA/思维链/对齐/算力/生成式AI/智能体编排/多智能体/世界模型/卫星互联网 等）；点击名词跳转"名词释义 + 架构含义(内联SVG) + 关联新闻"详情。
- [x] **架构索引 → 架构详情**：新增 `ARCH_NAMES` + `archIndex()`，列出全部 18 张架构图；点击看 SVG + 涉及名词 + 关联新闻；**名词 ↔ 架构 双向跳转**（架构详情的名词药丸可回跳名词释义）。
- [x] **新闻库**：可按分类(全部/AI圈/科技圈)筛选浏览全部新闻条目。
- [x] **资料库搜索**：覆盖 名词/架构/新闻 三类，命中分别列出；无结果给空态提示。
- [x] **术语弹层 → 资料库直链**：`#glossPop` 新增"在资料库查看 →"，点击直接进该术语详情。
- [x] **自动更新（每周）**：创建 automation（id `automation-1785031880063`，每周一 09:00）——检索新周真实新闻、按字段契约追加到 news-data.js、跑 r15/r14/r13 测试、重新部署 CloudStudio；资料库头部常驻"🔄 每周自动更新 · 当前数据：第N周"徽标。
- [x] **模拟验证（r15_test.cjs，jsdom 真机派发）**：**32/32 全绿**——资料库开关/名词详情含架构图与关联新闻/架构索引数=带架构新闻数/名词↔架构双向跳转/从资料库点新闻回资讯流/新闻库全量+AI筛选/搜索命中+空态/术语弹层+gp-more直链/返回资讯流；r14_test 13/13 + r13_test 31/31 零回归。
- [x] **重新部署** CloudStudio 生效（同一沙箱 ffab6d47...），分享链接不变；5 资源全 200。
- [x] 四问写入状态机（q3：从"新闻可读"升到"知识可查"，资料库是"学习性+新闻性"独特定位的真正落地）。

### 第十六轮候选（来自第十五轮四问，部分已在本轮落地）
- **还能做**：助手"本周导读"主动推送 / 技术演进时间线由助手讲解演变 / 资料库离线 PWA 缓存使助手无网答术语 / 助手生成"本周学习清单"导出。
- **能加**：分享长图把"助手对话 + 资料库术语卡 + 架构图"一并绘入 / 每架构补"一句话原理"摘要卡供助手引用 / 更多分类(生命医学/能源)真实数据纳入助手检索 / 多轮追问自动关联上文工具结果。
- **提升思维**：从"知识可查"(资料库)升到"知识可对话"——资料库是静态地图，助手是活的导游：自然语言触发索引操作找文章，密钥本地化让"私人 AI 导师"零成本零隐私顾虑。
- **能集成**：axe-core 确保 .agent-* 抽屉/弹层/工具卡片不破坏读屏 / html2canvas 把助手对话与资料库绘入分享长图 / model_router 调 LLM 作助手后端 / RSS·NewsAPI 抓取后灌入助手检索库。

### 2026-07-26 第十六轮（已完成 · 右侧 LLM 学习助手 — 用户："增添可点击 LLM Agent 助手，做真正有价值/独特/体验好的应用层"）
- [x] **右侧可对话助手入口**：头部新增 🤖助手 按钮；常驻可收缩右侧抽屉（`#agentPanel` 固定定位、移动端全屏 + 遮罩 `#agentOverlay`、明暗自适应），不干扰资讯流阅读。
- [x] **助手两大能力**：(a) 回答问题——权威严谨、可闲聊；(b) 索引操作——5 类工具 `search_news`/`lookup_term`/`lookup_architecture`/`open_article`/`save_note`，直接调用本站 `data`/`GLOSSARY`/`ARCH_IDX`；检索/查询结果以卡片呈现，一键跳正文或资料库详情（"通过索引操作去寻找文章"落地）。
- [x] **自定义配置弹层（四要求全覆盖）**：(a) **兼容格式**——接口地址 + 模型名 + 服务商预设(OpenAI/DeepSeek/Kimi/Qwen/Ollama)；(b) **可交互性**开关（是否允许调用资料库工具）；(c) **可确认 + 可索引到模型**——索引/导航操作先弹确认框征求同意，工具结果回灌模型做检索增强(RAG)；(d) **安全机制**——密钥仅存本机 `localStorage` 永不发往本站服务器（请求由浏览器直连用户接口）、记忆与密钥分离可保存、清除密钥不删记忆。
- [x] **应用层体验**：工具卡片透明展示"在做什么"、引用/结果卡片直跳原文、确认防误跳、建议气泡、流式输出(可选，SSE 解析)、对话与笔记本地持久（"每个人的记忆可以保存"落地）。
- [x] **修复两 bug**：`callModel` 未提取 OpenAI 格式 `tool_calls[].function.name`（导致工具名 undefined / 不执行）`renderAgentState` 启用后未清除初始"未配置"卡片。
- [x] **模拟验证（r16_test.cjs，jsdom 真机派发）**：**27/27 全绿**——配置保存使 enabled 且"去配置"消失 / search_news 触发确认框标注"检索新闻" / 结果卡片≥1 / 助手最终回答 / 对话记忆持久≥2条 / 点击卡片跳正文含该标题 / open_article 触发确认框标注"打开文章"且确认后跳目标文章 / save_note 确认后笔记入库+1 / 请求 URL=用户 baseURL+/chat/completions 且 Authorization=本地密钥 / 本站服务器不持密钥 / 清除密钥后本地无密钥且记忆保留；r15_test 32/32 + r14 13/13 + r13 31/31 零回归。
- [x] **重新部署** CloudStudio（同一沙箱 ffab6d47...），分享链接不变；5 资源全 200（部署后短暂网关传播竞态已恢复）。
- [x] 四问写入状态机（q3：从"知识可查"升到"知识可对话"，助手把"学习性+新闻性"做成可交互应用层的关键一跃）。

### 2026-07-26 第十七轮（已完成 · 发布到 GitHub Pages + 每周自动更新上线 — 用户："采用方案B 静态站+自动抓取0元，发布前检查优化后发布GitHub，确保每人可用+每周自动更新"）
- [x] **发布前最终检查**：r13 31/31 + r14 13/13 + r15 32/32 + r16 27/27 全绿（共 103 项）；敏感扫描确认无硬编码密钥（BYOK 模式安全）；确认 `.workbuddy/memory/` 内部状态不误提交。
- [x] **发布物料**：git init；`.gitignore`（排除 `.workbuddy/`·`.loop-state.json`·`node_modules/`）；`README.md`（独特定位/在线地址/本地运行/自动更新机制/隐私 BYOK/数据源/目录）；`package.json`（`npm test` 跑 r13–r16 + `npm run weekly-update`）；`scripts/weekly_update.cjs`（RSS 抓取 + 可选 DeepSeek AI 摘要 + 幂等追加新周 + serialize 写回）。
- [x] **DRY_RUN 验证脚本**：本地 `DRY_RUN=1 node scripts/weekly_update.cjs` 成功抓取 14 条候选、计算新周 w6、未污染主数据；AI 调用失败时正确 fallback 规则模板。
- [x] **创建公开仓库并推送**：`gh repo create huanweide/tech-news-hub --public`，git commit + push，默认分支 master。
- [x] **启用 GitHub Pages**：从 master/root 部署，`https://huanweide.github.io/tech-news-hub/` 构建成功、6 资源全 200（短暂 CDN 传播延迟后稳定）。
- [x] **两层自动更新上线**：① 更新 WorkBuddy 自动化 `automation-1785031880063`（每周一 09:00，本模型高质量更新 + git push，Pages 自动部署，原 CloudStudio 步骤移除）；② 新增 GitHub Actions `weekly-update.yml`（每周日 22:00 UTC，RSS 兜底 + 可选 `DEEPSEEK_API_KEY` AI 摘要 + push），`npm test` 为质量门。
- [x] 四问写入状态机（q3：从"本地可跑"升到"公开可访问+自动运维"，方案 B 把"非盈利科普"真正规模化——0 元托管 + 每周自动更新，任何人可访问、无需维护）。

### 第十八轮候选（来自第十七轮四问）
- **还能做**：axe-core 无障碍回归（确保抽屉/弹层/工具卡片不破坏读屏）/ 分享长图含助手对话与资料库 / 技术演进时间线 / 更多分类(生命医学·能源)真实数据。
- **能加**：在 GitHub 仓库 Settings→Secrets 配置 `DEEPSEEK_API_KEY` 后 Actions 启用 AI 摘要（README 已说明）/ 助手"本周导读"主动推送 / 资料库离线 PWA 缓存。
- **提升思维**：从"产品能用"升到"产品能自己活"——发布+自动更新让站点脱离人工维护，真正达成"非盈利科普规模化"。
- **能集成**：axe-core 无障碍回归 / html2canvas 分享长图 / RSS·NewsAPI 增强抓取（多源去重）/ model_router 调 LLM 作助手后端。

### 2026-07-26 第十八轮（已完成 · 模型优惠圈 — 用户："开发模型优惠圈，集中发布中转站与大模型平台优惠资讯，完整前后端"）
- [x] **独立视图 deals（仿资料库 #kbView 模式，零回归）**：顶栏新增 🎯优惠圈 按钮；`#dealsView` 替代资讯流；`deals-data.js` 8 条样例覆盖 4 类型（进行中 current / 新上线 new / 降价预告 pricecut / 性价比 value）× 2 平台（中转站 relay / 官方 official），含 1 条已过期(2026-07-10)与 1 条即将开始(2026-08-05)供测试；字段契约 `id/title/platformType/platform/type/summary/detail/validFrom/validUntil/price/valueScore/sourceUrl/tags/addedAt`。
- [x] **前端逻辑（客户端自动过期下架）**：`dealStatus` 计算 active/upcoming/ended(剩余天数)；`isDealExpired` 过期默认隐藏(可选显示)；按 类型/平台/关键词 过滤；排序 active→upcoming→ended + 性价比降序；`buildDealCard` 渐变卡(平台/类型/状态 徽标 · 价格 · 有效期 · 标签 · 来源跳转 · 详情手风琴)。
- [x] **样式**：`style.css` 新增优惠圈全套明暗自适应样式(`.deals-view`/`.deal-card`/`.deal-status`/`.deals-filters` 等)。
- [x] **后端脚本 `scripts/deals_update.cjs`（保留 0 元静态站模型）**：读取现有 `deals-data.js`(用 fake-window 求值避免污染全局)；从 `deals-sources.json` 收集候选；按 id 幂等合并(已存在跳过)；可选 DeepSeek AI 摘要(失败回退规则模板)；`DRY_RUN=1` 安全模式；无候选源时为安全 no-op。
- [x] **助手第 6 工具 `lookup_deal`**：`TOOLS` 新增；`agentLookupDeal` 按关键词/平台/类型检索；`execTool` 分支返回卡片(`go.type=deal`)；`confirmLabel` 标注"查询优惠"；`agentNav` 打开优惠圈；`SYSTEM_PROMPT` 告知可查优惠；r16_test 由 27→31(新增确认框/标注查询优惠/结果卡片≥1/点击卡片打开优惠圈 4 断言) 全绿。
- [x] **质量门接入**：`package.json` 的 `npm test` 加入 `deals_test.cjs`；`weekly-update.yml` 新增"同步模型优惠圈"步骤(可选 `DEEPSEEK_API_KEY`) + `git add deals-data.js`；全栈 r13 31/r14 13/r15 32/r16 31/deals 19 共 **126 项断言全绿**。
- [x] **模拟验证**：`deals_test.cjs`(jsdom 真机派发) 19/19 全绿——优惠圈按钮打开视图 `body.deals-open`、默认隐藏过期(计数=未过期数=7)、显示过期显示全部(8)、`type=value` 过滤、`platform=official` 过滤、硅基流动显示进行中、GPU券显示即将开始、详情手风琴展开、返回资讯流关闭视图；`r16_test` 31/31 零回归；`DRY_RUN=1 node scripts/deals_update.cjs` 输出 0 新增 + 当前有效优惠 7 条。
- [x] 四问写入状态机（q3：从"新闻可对话"升到"资讯可省钱"，自动过期下架即信息有时效性的工程化表达）。

### 第十九轮候选（来自第十八轮四问）
- **还能做**：优惠详情接入分享长图 / 优惠圈真实抓取(RSS/NewsAPI/各平台公告)自动灌入 deals-sources.json / 过期优惠独立归档区 / 降价日历视图 / 优惠与对应新闻关联。
- **能加**：中转站专属评级(稳定性·速度·价格) / 邀请官链接一键复制带推广码 / 用户投稿优惠(UGC 审核后入源) / 优惠到期前本地提醒 / 平台 Logo 图标 / 类型标签页。
- **提升思维**：从"新闻可对话"升到"资讯可省钱"——把站点从纯科普延展到帮读者真正薅羊毛/选对模型，是学习性+新闻性+实用性三位一体的落地。
- **能集成**：axe-core 无障碍回归 / html2canvas 分享长图含优惠圈 / DEEPSEEK_API_KEY 配后 Actions 自动 enrich / RSS/NewsAPI 增强抓取 / model_router 调 LLM 作助手后端。
